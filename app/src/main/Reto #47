public class vocalMasRepetida {

    /*
 * Reto #47
 * VOCAL MÁS COMÚN
 * Fecha publicación enunciado: 21/11/22
 * Fecha publicación resolución: 28/11/22
 * Dificultad: FÁCIL
 *
 * Enunciado: Crea un función que reciba un texto y retorne la vocal que más veces se repita.
 * - Ten cuidado con algunos casos especiales.
 * - Si no hay vocales podrá devolver vacío.
 *
 * Información adicional:
 * - Usa el canal de nuestro Discord (https://mouredev.com/discord) "🔁reto-semanal"
 *   para preguntas, dudas o prestar ayuda a la comunidad.
 * - Tienes toda la información sobre los retos semanales en
 *   https://retosdeprogramacion.com/semanales2022.
 *
 */
    public static void main(String[] args) {
        String palabra = "lkl";

        vocalRepetida(palabra);
    }

    public static void vocalRepetida(String palabra) {
        palabra.replace("//s", "").toLowerCase();
        int contador;
        ;

        char arr[] = palabra.toCharArray();
        char vocales[] = { 'a', 'e', 'i', 'o', 'u' };
        HashMap<Integer, Character> vocalMasRepetida = new HashMap<>();

        for (int i = 0; i < vocales.length; i++) {
            contador = -1;
            for (int j = 0; j < arr.length; j++) {
                if (arr[j] == vocales[i]) {
                    contador++;

                    vocalMasRepetida.put(contador, arr[j]);
                }

            }

        }

        Integer claveMayor = null;
        for (Integer clave : vocalMasRepetida.keySet()) {

            if (claveMayor == null || clave > claveMayor) {
                claveMayor= clave;
            }
            
        }
        if (claveMayor != null) {
            System.out.println(" la vocal que mas serepite es la: " + vocalMasRepetida.get(claveMayor));
        }else{
            System.out.println(" -- ") ;
        }
       
    }

}

